[workspace]
name = "proxsuite"
version = "0.7.2"
description = "The Advanced Proximal Optimization Toolbox"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]
preview = ["pixi-build"]

[dependencies]
ccache = ">=4.12.0"
cmake = ">=3.22"
cxx-compiler = ">=1.11.0"
ninja = ">=1.13.0"
eigen = ">=3.4.0"
simde = ">=0.8.2"

[activation.env]
# Setup ccache
CMAKE_C_COMPILER_LAUNCHER = "ccache"
CMAKE_CXX_COMPILER_LAUNCHER = "ccache"
# Create compile_commands.json for language server
CMAKE_EXPORT_COMPILE_COMMANDS = "ON"
# Activate color output with Ninja
CMAKE_COLOR_DIAGNOSTICS = "ON"
# Help ccache manage generated files and PCH (https://ccache.dev/manual/latest.html#_precompiled_headers)
CCACHE_SLOPPINESS = "include_file_ctime,include_file_mtime,pch_defines,time_macros"

[tasks]
configure = { cmd = [
  "cmake",
  "-S",
  ".",
  "-B",
  "build",
  "-G",
  "Ninja",
  "-DJRL_CMAKEMODULES_SOURCE_DIR=$JRL_CMAKEMODULES_SOURCE_DIR",
  "-DCMAKE_INSTALL_PREFIX=$CONDA_PREFIX",
  "-DCMAKE_BUILD_TYPE=Release",
  "-DPROXSUITE_CXX_STANDARD=$PROXSUITE_CXX_STANDARD",
  "-DBUILD_WITH_VECTORIZATION_SUPPORT=ON",
  "-DBUILD_MAROS_MESZAROS_TESTS=$BUILD_MAROS_MESZAROS_TESTS",
  "-DBUILD_EXAMPLES=ON",
  "-DBUILD_BENCHMARKS=ON",
  "-DBUILD_TESTING=$BUILD_TESTING",
  "-DBUILD_PYTHON_INTERFACE=$BUILD_PYTHON_INTERFACE",
] }
build = { depends-on = ["configure"], cmd = "cmake --build build" }
install = { depends-on = ["build"], cmd = "cmake --install build" }
uninstall = { depends-on = [
  "install",
], cmd = "cmake --build build --target uninstall" }
clean = { cmd = "rm -rf build" }

# --- Features ---
# Each feature groups optional dependencies and tasks.
# They are composed together via environments (see [environments] below).

# --- Python Feature ---
# Adds Python bindings support with nanobind.
# Enables building and testing Python wrappers for the C++ library.
[feature.python.dependencies]
python = ">=3.10"
numpy = ">=1.24.0"
nanobind = ">=2.5.0"

[feature.python.activation.env]
BUILD_PYTHON_INTERFACE = "ON"

[feature.python.tasks]
test-import-python = { depends-on = [
  "install",
], cmd = [
  "python",
  "-c",
  "import proxsuite; print(proxsuite.helpers.printVersion())",
] }

# --- Test Feature ---
# Adds C++ testing dependencies and test runners.
# Includes support for Maros-Meszaros benchmark tests (very slow, excluded by default).
# Use `test` task for regular tests, `test_maros_meszaros` for benchmark tests.
[feature.test.dependencies]
catch2 = ">=3.4.0"
cereal = ">=1.3.0"
scipy = ">=1.7.0"
libmatio = ">=1.5.0"

[feature.test.activation.env]
BUILD_TESTING = "ON"
BUILD_MAROS_MESZAROS_TESTS = "ON"

[feature.test.tasks]
test = { depends-on = [
  "build",
], cmd = [
  "ctest",
  "--test-dir",
  "build",
  "-LE",
  "maros_meszaros",
  "--output-on-failure",
] }
test_maros_meszaros = { depends-on = [
  "build",
], cmd = [
  "ctest",
  "--test-dir",
  "build",
  "-L",
  "maros_meszaros",
  "--output-on-failure",
] }
_test-packaging-simple-configure = { depends-on = [
  "install",
], cmd = [
  "cmake",
  "-G",
  "Ninja",
  "-S",
  "test/packaging/cmake",
  "-B",
  "build/packaging",
  "-DJRL_CMAKEMODULES_SOURCE_DIR=$JRL_CMAKEMODULES_SOURCE_DIR",
  "--log-level=DEBUG",
] }
_test-packaging-simple = { depends-on = [
  "_test-packaging-simple-configure",
], cmd = [
  "cmake",
  "--build",
  "build/packaging",
  "--verbose",
] }
_test-packaging-components-configure = { depends-on = [
  "install",
], cmd = [
  "cmake",
  "-G",
  "Ninja",
  "-S",
  "test/packaging/cmake-components",
  "-B",
  "build/packaging-components",
  "-DJRL_CMAKEMODULES_SOURCE_DIR=$JRL_CMAKEMODULES_SOURCE_DIR",
  "--log-level=DEBUG",
] }
_test-packaging-components = { depends-on = [
  "_test-packaging-components-configure",
], cmd = [
  "cmake",
  "--build",
  "build/packaging-components",
  "--verbose",
] }
_test-packaging-fetchcontent-configure = { cmd = [
  "cmake",
  "-G",
  "Ninja",
  "-S",
  "test/packaging/fetchcontent",
  "-B",
  "build/packaging-fetchcontent",
  "-DJRL_CMAKEMODULES_SOURCE_DIR=$JRL_CMAKEMODULES_SOURCE_DIR",
  "-DPROXSUITE_FETCHCONTENT_SOURCE_DIR=$PIXI_PROJECT_ROOT",
  "--log-level=DEBUG",
] }
_test-packaging-fetchcontent = { depends-on = [
  "_test-packaging-fetchcontent-configure",
], cmd = [
  "cmake",
  "--build",
  "build/packaging-fetchcontent",
  "--verbose",
] }
test-packaging = { depends-on = [
  "_test-packaging-simple",
  "_test-packaging-components",
  "_test-packaging-fetchcontent",
] }

# --- Documentation Feature ---
# Standalone environment for building documentation with Doxygen.
# Use `build_documentation` to generate docs, `install_documentation` to install them.
[feature.doc.dependencies]
doxygen = ">=1.10.0"
graphviz = ">=10.0.0"

[feature.doc.tasks]
_configure_documentation = { cmd = [
  "cmake",
  "-S",
  ".",
  "-B",
  "build/doc_build",
  "-G",
  "Ninja",
  "-DJRL_CMAKEMODULES_SOURCE_DIR=$JRL_CMAKEMODULES_SOURCE_DIR",
  "-DCMAKE_INSTALL_PREFIX=build/doc_install",
  "-DCMAKE_BUILD_TYPE=Release",
  "-DBUILD_DOCUMENTATION=ON",
  "-DINSTALL_DOCUMENTATION=ON",
] }
build_documentation = { depends-on = [
  "_configure_documentation",
], cmd = [
  "cmake",
  "--build",
  "build/doc_build",
  "--target",
  "doc",
] }
install_documentation = { depends-on = [
  "build_documentation",
], cmd = [
  "cmake",
  "--install",
  "build/doc_build",
  "--component",
  "doc",
] }

# --- Environments ---
# Environments compose features together for different use cases.
[environments.default]
features = ["python", "test"]

[environments.doc]
features = ["doc"]

# --- lint Feature ---
# Standalone environment using lint.
# Minimal dependencies - only includes lint.
[feature.lint.dependencies]
pre-commit = "*"

[feature.lint.tasks]
lint = { cmd = "pre-commit run -a" }

[environments.lint]
features = ["lint"]
no-default-feature = true

# --- Pixi Build Feature ---
# Standalone environment for building with Pixi.
[package]
name = { workspace = true }
version = { workspace = true }

[package.build]
backend = { name = "pixi-build-cmake", version = "*" }

[package.build.config]
extra-args = [
  "--log-level=DEBUG",
  "-DBUILD_WITH_VECTORIZATION_SUPPORT=ON",
  "-DBUILD_EXAMPLES=OFF",
  "-DBUILD_BENCHMARKS=OFF",
  "-DBUILD_TESTING=OFF",
  "-DBUILD_MAROS_MESZAROS_TESTS=OFF",
  "-DBUILD_PYTHON_INTERFACE=ON",
]

[package.build-dependencies]
ccache = ">=4.12.0"
cmake = ">=3.22"
cxx-compiler = ">=1.11.0"
ninja = ">=1.13.0"

[package.host-dependencies]
eigen = ">=3.4.0"
simde = ">=0.8.2"
python = ">=3.10"
numpy = ">=1.24.0"
nanobind = ">=2.5.0"
cereal = ">=1.3.0"

[package.run-dependencies]
eigen = ">=3.4.0"
simde = ">=0.8.2"
cereal = ">=1.3.0"

# [feature.test-pixi-build.dependencies]
# proxsuite = { path = "." }
# cmake = ">=3.22"
# python = ">=3.10"
# cxx-compiler = ">=1.11.0"

# [feature.test-pixi-build.tasks]
# test-cmake = "cmake -S test/packaging/cmake -B build/test_pixi_build --log-level=DEBUG"
# test-python = "python -c 'import proxsuite; print(proxsuite.__version__)'"
# test = { depends-on = ["test-cmake", "test-python"] }

# [environments.test-pixi-build]
# features = ["test-pixi-build"]
# no-default-feature = true
